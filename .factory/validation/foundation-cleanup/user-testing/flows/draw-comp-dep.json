{
  "groupId": "draw-comp-dep",
  "testedAt": "2026-03-15T00:00:00Z",
  "isolation": {
    "codebaseRoot": "/Users/user/Sites/os",
    "systemRoot": "/Users/user/Sites/os/system",
    "mode": "read-only",
    "buildVerified": "parent validator confirmed build exit 0, 1462 tests pass"
  },
  "toolsUsed": ["grep", "ls"],
  "assertions": [
    {
      "id": "VAL-DRAW-001",
      "title": "Dead rasterizer types removed",
      "status": "pass",
      "steps": [
        {
          "action": "grep -n 'struct Segment|struct ActiveEdge|MAX_SEGMENTS|MAX_ACTIVE_EDGES' system/libraries/drawing/rasterizer.rs",
          "expected": "Zero matches (exit code 1)",
          "observed": "Zero matches (exit code 1)"
        }
      ],
      "evidence": {
        "commandOutput": "foundation-cleanup/draw-comp-dep/VAL-DRAW-001-grep-output.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-COMP-005",
      "title": "Compositor uses fonts crate not shaping",
      "status": "pass",
      "steps": [
        {
          "action": "grep -rn 'shaping' system/services/compositor/*.rs",
          "expected": "Zero matches (exit code 1)",
          "observed": "Zero matches (exit code 1)"
        }
      ],
      "evidence": {
        "commandOutput": "foundation-cleanup/draw-comp-dep/VAL-COMP-005-grep-output.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEP-001",
      "title": "No circular library dependencies",
      "status": "pass",
      "steps": [
        {
          "action": "grep -rn 'extern crate' in each library under system/libraries/",
          "expected": "DAG with no cycles: scene has no deps, fonts depends on alloc only, drawing depends on alloc+fonts, no library depends on a service",
          "observed": "scene: alloc only. fonts: alloc only. drawing: alloc + fonts. ipc: none. protocol: none. sys: none. virtio: none. No cycles, no service dependencies."
        }
      ],
      "evidence": {
        "commandOutput": "foundation-cleanup/draw-comp-dep/VAL-DEP-001-dependency-graph.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEP-002",
      "title": "All extern crate shaping references eliminated",
      "status": "pass",
      "steps": [
        {
          "action": "grep -rn 'extern crate shaping' system/ --include='*.rs' (excluding target/ and .factory/)",
          "expected": "Zero matches (exit code 1)",
          "observed": "Zero matches (exit code 1)"
        }
      ],
      "evidence": {
        "commandOutput": "foundation-cleanup/draw-comp-dep/VAL-DEP-002-grep-output.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEP-003",
      "title": "No stale shaping directory or references",
      "status": "pass",
      "steps": [
        {
          "action": "ls system/libraries/shaping",
          "expected": "Directory does not exist (exit code 1)",
          "observed": "No such file or directory (exit code 1)"
        },
        {
          "action": "grep -rn 'libraries/shaping' system/ (excluding target/ and .factory/)",
          "expected": "Zero matches (exit code 1)",
          "observed": "Zero matches (exit code 1)"
        }
      ],
      "evidence": {
        "commandOutput": "foundation-cleanup/draw-comp-dep/VAL-DEP-003-check-output.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEP-004",
      "title": "Scene library remains dependency-free",
      "status": "pass",
      "steps": [
        {
          "action": "grep -n 'extern crate' system/libraries/scene/lib.rs",
          "expected": "Only 'extern crate alloc' found",
          "observed": "Line 32: extern crate alloc; — only match"
        }
      ],
      "evidence": {
        "commandOutput": "foundation-cleanup/draw-comp-dep/VAL-DEP-004-grep-output.txt"
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 6 assertions: 6 passed, 0 failed. Dead rasterizer types removed, compositor fully migrated from shaping to fonts, library dependency graph is a clean DAG with no cycles, all shaping references eliminated, shaping directory removed, and scene library remains dependency-free."
}
