{
  "groupId": "cli-core-dead-build",
  "testedAt": "2026-03-15T00:00:00Z",
  "isolation": {
    "surface": "CLI (shell commands: grep, ls, file inspection)",
    "mode": "READ-ONLY",
    "directory": "/Users/user/Sites/os/system/"
  },
  "toolsUsed": ["Execute"],
  "assertions": [
    {
      "id": "VAL-CORE-005",
      "title": "TextLayout owned by Core after milestone 2",
      "status": "pass",
      "steps": [
        {
          "action": "grep -rn 'drawing::TextLayout' system/services/core/*.rs",
          "expected": "No matches (exit code 1)",
          "observed": "No matches (exit code 1). TextLayout is not imported from drawing in core."
        }
      ],
      "evidence": {
        "commandOutput": "grep returned exit 1 (no matches)",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEAD-001",
      "title": "No orphaned include! directives",
      "status": "pass",
      "steps": [
        {
          "action": "grep -n 'include!' system/libraries/drawing/lib.rs",
          "expected": "List of include! directives",
          "observed": "Two include! directives: gamma_tables.rs (line 24), palette.rs (line 25)"
        },
        {
          "action": "ls -la for each include! target file",
          "expected": "All referenced files exist",
          "observed": "gamma_tables.rs (23162 bytes) and palette.rs (3292 bytes) both exist"
        }
      ],
      "evidence": {
        "commandOutput": "include!(\"gamma_tables.rs\") -> EXISTS (23162 bytes); include!(\"palette.rs\") -> EXISTS (3292 bytes)",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEAD-002",
      "title": "No orphaned imports in test files",
      "status": "pass",
      "steps": [
        {
          "action": "grep -rn 'use shaping::' system/test/tests/",
          "expected": "No matches (exit 1)",
          "observed": "No matches (exit 1). No old shaping imports remain."
        },
        {
          "action": "grep -rn for drawing::GlyphCache, drawing::TextLayout, etc. in test files",
          "expected": "No matches (exit 1)",
          "observed": "No matches (exit 1). No references to symbols removed from drawing."
        }
      ],
      "evidence": {
        "commandOutput": "Both greps returned exit 1 (no matches). All 1462 tests compile and pass.",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEAD-003",
      "title": "No orphaned references in services",
      "status": "pass",
      "steps": [
        {
          "action": "grep -rn 'drawing::GlyphCache|drawing::TextLayout|drawing::DamageTracker|drawing::CompositeSurface|drawing::render_cursor|drawing::scale_pointer_coord|drawing::svg_|drawing::png_|drawing::SvgPath|drawing::PngHeader' system/services/core/*.rs system/services/compositor/*.rs",
          "expected": "No matches (exit 1)",
          "observed": "No matches (exit 1). No orphaned references to removed drawing types in any service file."
        }
      ],
      "evidence": {
        "commandOutput": "grep returned exit 1 (no matches). Full build succeeds (exit 0).",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEAD-004",
      "title": "No unused imports after refactoring",
      "status": "pass",
      "steps": [
        {
          "action": "cd system && cargo build --release 2>&1 | grep -i 'unused import'",
          "expected": "No matches (grep exit 1)",
          "observed": "No matches (grep exit 1). Zero unused import warnings."
        }
      ],
      "evidence": {
        "commandOutput": "cargo build --release completed. grep 'unused import' returned exit 1 (no matches).",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-DEAD-005",
      "title": "palette.rs constants accessible",
      "status": "pass",
      "steps": [
        {
          "action": "Verify palette.rs is included in drawing/lib.rs",
          "expected": "include!(\"palette.rs\") present",
          "observed": "Line 25: include!(\"palette.rs\") present in drawing/lib.rs"
        },
        {
          "action": "grep for palette constants (CHROME_BG, BG_BASE, TEXT_PRIMARY, etc.) in core service",
          "expected": "Constants used via drawing:: prefix",
          "observed": "Found 6 references: drawing::CHROME_BG, drawing::BG_BASE, drawing::TEXT_PRIMARY in core/main.rs"
        },
        {
          "action": "grep for palette constants in compositor service",
          "expected": "Constants used via drawing:: prefix",
          "observed": "Found 2 references: drawing::CURSOR_FILL, drawing::CURSOR_OUTLINE in compositor/cursor.rs"
        },
        {
          "action": "Verify services compile",
          "expected": "cargo build --release exits 0",
          "observed": "Build exits 0. All palette constants resolve correctly."
        }
      ],
      "evidence": {
        "commandOutput": "Core uses drawing::CHROME_BG, drawing::BG_BASE, drawing::TEXT_PRIMARY. Compositor uses drawing::CURSOR_FILL, drawing::CURSOR_OUTLINE. Build succeeds.",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-BUILD-001",
      "title": "Full cargo build --release succeeds",
      "status": "pass",
      "steps": [
        {
          "action": "cd system && cargo build --release",
          "expected": "Exit code 0",
          "observed": "Finished `release` profile [optimized] target(s). Exit code 0."
        }
      ],
      "evidence": {
        "commandOutput": "Finished `release` profile [optimized] target(s) in 0.00s. EXIT: 0",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-TEST-001",
      "title": "All tests pass (count >= 1462)",
      "status": "pass",
      "steps": [
        {
          "action": "cd system/test && cargo test -- --test-threads=1",
          "expected": "0 failures, total >= 1462",
          "observed": "54 test result lines, all ok. 0 failed. 0 ignored. Total passed: 1462."
        }
      ],
      "evidence": {
        "commandOutput": "Total passed: 1462. 0 failed. 0 ignored. Exit code 0.",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    },
    {
      "id": "VAL-TEST-002",
      "title": "No #[ignore] additions",
      "status": "pass",
      "steps": [
        {
          "action": "grep -rn '#[ignore]' system/test/tests/",
          "expected": "No matches or count matches pre-refactor baseline",
          "observed": "No matches (exit 1). Zero #[ignore] annotations in test files."
        }
      ],
      "evidence": {
        "commandOutput": "grep returned exit 1 (no matches). Zero tests suppressed.",
        "logFile": "drawing-consolidation/cli-core-dead-build/evidence-log.txt"
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 9 assertions: all 9 passed. Build succeeds (exit 0), 1462 tests pass with 0 failures and 0 ignored, no orphaned include! directives, no orphaned imports or references, no unused imports, palette constants accessible from both core and compositor services."
}
