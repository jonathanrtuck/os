{
  "flowId": "lock-ordering",
  "testedAt": "2026-03-11T00:00:00Z",
  "assertions": {
    "VAL-CROSS-001": {
      "status": "pass",
      "evidence": "system/kernel/LOCK-ORDERING.md exists (test -f exits 0). File documents 13 distinct IrqMutex instances across 13 files in a detailed inventory table (Section 1). 20 IrqMutex references total in the file (>= 16 lock sites). Section 2 provides a lock ordering DAG with 4 ordering levels. Section 3 enumerates 12 multi-lock code paths with acquisition order constraints. Commit ab37394 'audit(lock-ordering): cross-file lock ordering analysis — 13 IrqMutex instances, no circular dependencies' confirms the analysis. grep -c 'lock.*order|Lock.*Order' system/kernel/DESIGN.md returns 5 additional references."
    },
    "VAL-CROSS-002": {
      "status": "pass",
      "evidence": "LOCK-ORDERING.md Section 5 'Circular Dependency Analysis' explicitly verifies every lock pair edge in the DAG and confirms no reverse edge exists. Table shows 10 forward edges, all verified with 'No' for reverse edge. Section 5 concludes: 'Result: No circular dependencies found. The lock ordering forms a strict DAG.' Section 6 summary confirms: 'Circular dependencies: None found', 'Lock ordering violations: None found'. Allocation-induced transitive nesting (scheduler → ALLOC_LOCK → SLAB → page_allocator) is analyzed and confirmed consistent. Commit message ab37394 explicitly states 'no circular dependencies'."
    }
  },
  "frictions": [],
  "blockers": [],
  "toolsUsed": ["terminal"]
}
