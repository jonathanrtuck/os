[package]
name = "mkdisk"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mkdisk"
path = "main.rs"

[dependencies]
fs = { path = "../../libraries/fs" }
piecetable = { path = "../../libraries/piecetable" }
store = { path = "../../libraries/store" }
