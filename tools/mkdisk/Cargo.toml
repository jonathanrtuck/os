[package]
name = "mkdisk"
version = "0.1.0"
edition = "2024"
license = "Unlicense"

[dependencies]
fs = { path = "../../user/libraries/fs" }
store = { path = "../../user/libraries/store" }

[workspace]
