[package]
name = "mkdisk"
version = "0.1.0"
edition = "2024"
license = "Unlicense"

[dependencies]
fs = { path = "../../user/shared/fs" }
manifest = { path = "../../user/shared/manifest" }
store = { path = "../../user/shared/store" }

[workspace]
