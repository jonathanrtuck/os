[workspace]
members = ["kernel"]
exclude = ["kernel/fuzz", "userspace/init", "userspace/abi"]
resolver = "3"

[profile.dev]
panic = "abort"
opt-level = 1

[profile.release]
panic = "abort"
opt-level = 3
