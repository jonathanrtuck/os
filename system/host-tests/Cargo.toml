[package]
name = "host-tests"
version = "0.1.0"
edition = "2021"

# Host-side unit tests for pure kernel modules (handle table, ELF parser).
# Lives outside system/kernel/ to avoid inheriting its aarch64-unknown-none
# target config. Run with `cargo test` from this directory.
