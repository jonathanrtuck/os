[package]
name = "ipc"
version = "0.1.0"
edition = "2024"
license = "Unlicense"

[dependencies]
abi = { path = "../abi", optional = true }

[features]
default = ["kernel"]
kernel = ["dep:abi"]

[workspace]
