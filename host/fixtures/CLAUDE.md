# test/fixtures

Test data files used by the host-side test suite.

## Subdirectories

- `pngsuite/` -- 180 PNG test images from the PngSuite conformance set (all color types, bit depths, filter methods, interlacing, palettes, transparency, corrupt files)
